<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.tm.taxi"
    android:versionCode="3"
    android:versionName="0.9.9" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"

        android:theme="@style/AppTheme" >
        <activity
            android:name="ru.tm.taxi.layout.MainActivity"
            android:screenOrientation="portrait"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * -->
        <activity
            android:name="ru.tm.taxi.layout.EnterLayout"
             />
        <!-- * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * -->
        <activity
            android:name="ru.tm.taxi.layout.ChoiceLayout"
            >
        </activity>
        <!-- * * * * * * * * * * * * * * * * *
        TODO  add android:screenOrientation="portrait"
        * * * * * * * * * * * * * * * * * * * * * * * * * * * * -->
        <activity
            android:name="ru.tm.taxi.layout.OrderLayout"
            android:label="@string/title_activity_order_layout"
            >
        </activity>
        <!-- * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * -->
        <activity
            android:name="ru.tm.taxi.layout.ConfirmLayout"
            android:label="@string/title_activity_confirm_layout" >
        </activity>
        <!-- * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * -->
        <activity
            android:name="ru.tm.taxi.layout.HistoryLayout"
            android:label="@string/title_activity_history_layout" >
        </activity>
        <activity android:name="ru.tm.taxi.layout.SettingLayout"/>
    </application>

</manifest>